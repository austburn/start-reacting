{
  "name": "start-reacting",
  "version": "1.0.0",
  "description": "A starter template for developing React applications",
  "scripts": {
    "bundle": "browserify -t reactify -e ./app/index.jsx -o ./dist/bundle.js",
    "server": "npm run bundle && http-server ./dist"
  },
  "author": "Austin Burnett",
  "devDependencies": {
    "browserify": "^8.1.3",
    "react": "^0.12.2",
    "reactify": "^1.0.0"
  }
}
